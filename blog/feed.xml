<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ar-forum.github.io/blog/feed.xml" rel="self" type="application/atom+xml" /><link href="https://ar-forum.github.io/blog/" rel="alternate" type="text/html" /><updated>2025-10-17T03:40:39-04:00</updated><id>https://ar-forum.github.io/blog/feed.xml</id><title type="html">AR Forum Blog</title><subtitle>The official blog of AR Forum - sharing insights, research, and updates on augmented reality technology and applications.</subtitle><entry><title type="html">Don’t (Just) Train For Performance</title><link href="https://ar-forum.github.io/blog/dont-just-train-for-performance/" rel="alternate" type="text/html" title="Don’t (Just) Train For Performance" /><published>2025-10-16T20:00:00-04:00</published><updated>2025-10-16T20:00:00-04:00</updated><id>https://ar-forum.github.io/blog/dont-just-train-for-performance</id><content type="html" xml:base="https://ar-forum.github.io/blog/dont-just-train-for-performance/"><![CDATA[<p>You’ve probably seen confident claims on social media by prominent AI personalities that we already have the ingredients for AGI and simply need to scale. Proponents of this claim often point to one of many graphs illustrating the release date of a model plotted against some measure of performance, showing an exponential grown in capability over time. It’s true that today’s language models are impressively capable. However, as I will argue in this post, when you actually try to use language models in practice, especially when you want to <em>adapt</em> them to new tasks, the picture becomes messier.</p>

<h2 id="performance-isnt-the-whole-story">Performance isn’t the whole story</h2>

<p>Imagine a student fresh out of their very first course on machine learning who decides to fine-tune a few open LMs to solve math word problems. They decide to train on the TinyGSM dataset, which contains examples of math word problems and associated Python programs that compute the solution.</p>

<div class="code-grid">
  <section>
    <h3>Example Prompt (TinyGSM)</h3>
    <pre class="prompt"><code class="nohighlight">A company has $50,000 in profits and wants to pay a bonus to its employees. If the company has 25 employees and the total bonus is $10,000, how much will each employee receive?</code></pre>
  </section>

  <section>
    <h3>Solution</h3>
    <pre><code class="language-python">def simple_math_problem() -&gt; int:
    '''
    A company has $50,000 in profits and wants to pay a bonus to its employees.
    If the company has 25 employees and the total bonus is $10,000, how much will each employee receive?
    '''
    bonus_per_employee = 10000 / 25
    result = bonus_per_employee
    return result</code></pre>
  </section>
</div>

<p>To limit the number of generated tokens, they remove the docstrings and comments from the solution before fine-tuning.</p>

<div class="code-grid">
  <section>
    <h3>Example Prompt (TinyGSM-No-Comments)</h3>
    <pre class="prompt"><code class="nohighlight">A company has $50,000 in profits and wants to pay a bonus to its employees. If the company has 25 employees and the total bonus is $10,000, how much will each employee receive?</code></pre>
  </section>

  <section>
    <h3>Solution (without comments)</h3>
    <pre><code class="language-python">def simple_math_problem() -&gt; int:
    bonus_per_employee = 10000 / 25
    result = bonus_per_employee
    return result</code></pre>
  </section>
</div>

<p>To fine-tune the model, they do everything their class taught them—construct a train/validation split, sweep over a reasonable range of hyperparameters, and then pick the best model based on validation loss. Using this approach, they fine-tune Qwen2.5-3B and Gemma-2B on ~2,000 examples from this modified TinyGSM. After training, they generate from each model and compare the results. Below are sample generations from the models.</p>

<h3 id="example-prompt">Example Prompt</h3>

<pre class="prompt">
Paul wants to buy ingredients for his grilled cheese sandwich. He needs 2 slices of bread that cost $2 each, 1 slice of cheese that costs $3, and some butter that cost $2. How much money does Paul need to buy the ingredients for the grilled cheese sandwich?
</pre>

<h3 id="model-generations-after-fine-tuning">Model generations after fine-tuning</h3>

<div class="code-grid">
  <section>
    <h3>Qwen2.5-3B</h3>
    <pre><code class="language-python">def simple_math_problem() -&gt; int:
    bread = 2 * 2
    cheese = 3
    butter = 2
    total = bread + cheese + butter
    result = total
    return result</code></pre>
  </section>

  <section>
    <h3>Gemma-2B</h3>
    <pre><code class="language-python">def simple_math_problem() -&gt; int:
    '''
    Paul wants to buy ingredients for his grilled cheese sandwich.
    He needs 2 slices of bread that cost $2 each, 1 slice of cheese that costs $3, and some butter that costs $2.
    How much money does Paul need to buy the ingredients for the grilled cheese sandwich?
    '''
    bread_cost = 2
    cheese_cost = 3
    butter_cost = 2
    num_slices_of_bread = 2
    num_slices_of_cheese = 1
    total_cost = bread_cost * num_slices_of_bread + cheese_cost * num_slices_of_cheese + butter_cost
    result = total_cost
    return result</code></pre>
  </section>
</div>

<p><strong>Surprise!</strong> For Gemma-2B, the docstrings appear, <em>even though the fine-tuning data never included them!</em> Of course, this is inevitably a result of Gemma-2B containing docstrings in its pre-training or mid-training data. But, this raises an important question: why isn’t a small format change easy to fine-tune?</p>

<p>It turns out, with not <em>that</em> much effort, it is possible to fine-tune Gemma-2B to not include the docstrings, e.g., with better hyperparameter tuning or more data. However, when even open-source models can typically interpret half-formed typo-laden prompts with ease, other aspects, such as adapting the language model <em>should just work</em>.</p>

<h2 id="adaptability-as-a-first-class-goal">Adaptability as a first-class goal</h2>

<p>The student’s struggle reflects a broad issue surrounding language models: <strong>adaptability</strong>. When we post-train or fine-tune, we generally want a model that is both:</p>

<ul>
  <li><strong>Plastic:</strong> Adapts easily and reach high downstream performance on the new task.</li>
  <li><strong>Robust:</strong> Retains the broad capabilities learned during pre-training.</li>
</ul>

<p>Together, these properties are what I will refer to as <em>adaptability</em>.</p>

<p>There’s a lot of evidence that larger models are both more plastic and more robust. It’s tempting to conclude that scaling grants adaptability “for free”. However, as we’ll see, this is not the case when we scale models without increasing their parameter count.</p>

<h2 id="the-inference-time-wall">The inference-time wall</h2>

<p>Language models are increasingly used for reasoning and agentic tasks, where the tasks demand sometimes thousands or tens of thousands of generated tokens for a single query, making efficient inference-time generation ever more relevant. Moreover, this figure is likely to increase as research and capability progresses.</p>

<p>This demands for the use of tiny language models. However, in order to compensate for their small size, these models are <em>overtrained</em>, consuming orders of magnitudes more tokens than the classical Chinchilla scaling laws predict as “train-compute optimal.”</p>

<p>The challenge, as we shall see, is that overtraining is terrible for adaptability.</p>

<h2 id="case-study-post-training-olmo-1b-across-its-trajectory">Case study: post-training OLMo-1B across its trajectory</h2>

<p>Consider checkpoints along the <a href="https://arxiv.org/abs/2402.00838">OLMo-1B</a> training trajectory, from early pre-training to the final 1.3T-token model. For each checkpoint, we track:</p>

<ol>
  <li>The base model’s general capability, measured by an average score on standard LLM benchmarks.</li>
  <li>The post-trained model’s general capability after instruction tuning (robustness proxy).</li>
  <li>The post-trained model’s instruction-following quality (plasticity proxy).</li>
</ol>

<figure>
<img src="/assets/images/overtraining/overtraining-olmo.png" alt="Overtraining effects across the OLMo-1B training trajectory" />
<figcaption>Figure 2. Overtraining vs. adaptability along the OLMo-1B trajectory.</figcaption>
</figure>

<p>Ideally, all three improve with more pre-training. In practice, early checkpoints improve on both instruction following and general capabilities, but beyond a point (≈2.3T tokens in our narrative) both <em>decline</em>, despite the base model’s pre-training loss still improving (Figure 2). We call this <em>catastrophic overtraining</em>.</p>

<blockquote>
  <p><strong>Definition.</strong> <em>Catastrophic overtraining</em> is the phenomenon where extending pre-training continues to improve base-model loss but <em>reduces</em> the adaptability and robustness achievable via post-training.</p>
</blockquote>

<h2 id="why-does-catastrophic-overtraining-happen">Why does catastrophic overtraining happen?</h2>

<p>To begin to address this question, we turn to a much simpler setting, where we update the model by adding Gaussian noise to the weights. Unsurprisingly, when adding Gaussian noise to the model, its performance—measured by the loss on web data—degrades. However, we can track by how much the performance degrades as a function of the number of tokens the model was pre-trained with.</p>

<figure>
<img src="/assets/images/overtraining/overtraining-gaussian.png" alt="Performance degradation vs. Gaussian noise magnitude across pretraining tokens" />
<figcaption>Figure 3. Degradation under fixed-magnitude Gaussian weight noise across checkpoints.</figcaption>
</figure>

<p>For a fixed perturbation magnitude, the amount by which the perplexity of the model degrades increases progressively throughout training. In effect, the model becomes progressively more sensitive to perturbations to its weights as it is pre-trained for longer (Figure 1). We call this <em>progressive sensitivity</em>.</p>

<p>At the same time, the performance of the base model improves as we pre-train for longer—so what’s going on? As training progresses, the rate at which the base model improves slows down, while the rate at which the model’s sensitivity increases speeds up. This means that early in training, when sensitivity is still small, it has a negligible effect on the model, and the performance of the perturbed model improves with training. Later in training, however, the sensitivity of the model dominates the overall loss, leading to a degradation of performance with additional pre-training.</p>

<figure>
<img src="/assets/images/overtraining/overtraining-intuition.png" alt="Schematic intuition for progressive sensitivity and performance trade-off" />
<figcaption>Figure 4. Schematic of progressive sensitivity and its interaction with base-model improvement.</figcaption>
</figure>

<p>Crucially, this phenomenon depends on the fact that we kept the magnitude of the perturbation constant for all checkpoints. Smaller perturbations lead to a smaller degradation of the loss, and so if the size of the perturbation were to decrease at the same time as sensitivity increases, then the sensitivity term may never dominate the overall performance, and performance would increase indefinitely.</p>

<h2 id="translating-this-intuition-to-the-fine-tuning-setting">Translating this intuition to the fine-tuning setting</h2>

<p>Our intuition—that the progressive increase in sensitivity eventually causes the performance of the perturbed model to degrade as sensitivity begins to dominate—nearly carries over to the fine-tuning setting, but we’re still missing one crucial piece of the puzzle. The perturbation to the weights from fine-tuning will not necessarily be the same magnitude for different pre-training checkpoints, and therefore we cannot conclude that the loss will necessarily eventually increase.</p>

<p>As it turns out, there is a setting where fine-tuning different checkpoints will yield a (relatively) consistent perturbation magnitude. In particular, this tends to occur when the fine-tuning hyperparameters—especially the peak fine-tuning learning rate—are set to a fixed (untuned) value for all pre-training checkpoints.</p>

<figure>
<img src="/assets/images/overtraining/overtraining-finetuning.png" alt="U-shaped loss curve after fixed-LR post-training across checkpoints" />
<figcaption>Figure 5. Fixed learning-rate post-training yields a U-shaped loss vs. token budget.</figcaption>
</figure>

<p>In this setting, where we don’t tune the hyperparameters for each checkpoint individually and instead use the same fine-tuning learning rate when post-training all checkpoints, we observe the exact trend we noticed when we perturbed the model with Gaussian noise.</p>

<p>Once again, the degradation induced by fine-tuning increases progressively throughout pre-training, leading to an overall “U”-shaped curve in the loss when evaluating the web-data perplexity of the post-trained model.</p>

<h2 id="what-happens-when-you-tune-the-learning-rate">What happens when you tune the learning rate?</h2>

<p>In practice, we would never pick a fixed set of hyperparameters. Rather, we would tune our hyperparameters on some downstream task to maximize the performance of the model. Since smaller learning rates lead to smaller fine-tuning perturbations, does this mean that catastrophic overtraining will disappear?</p>

<p>In fact, with our new intuition, we can now revisit our original question: for which datasets does catastrophic overtraining occur? It is exactly the training datasets where achieving strong downstream performance requires larger learning rates. For datasets where the tuned learning rate decreases sufficiently quickly as pre-training is extended, the increase in sensitivity is negated by the decrease in perturbation size. However, for datasets where the tuned learning rate remains sufficiently large, we observe degradation.</p>

<figure>
<img src="/assets/images/overtraining/overtraining-tuning.png" alt="When tuned learning rates mitigate or reveal catastrophic overtraining" />
<figcaption>Figure 6. When tuned LRs shrink perturbations fast enough, degradation vanishes; otherwise, it persists.</figcaption>
</figure>

<p>In total, we’ve established that overtraining can hurt the robustness and the adaptability of the model, and we’ve attributed this to a progressive increase in sensitivity to parameter perturbations that occurs as we pre-train the foundation model for longer. Thus, as long as the magnitude of the update from fine-tuning remains large enough not to cancel out the increase in sensitivity, we will observe catastrophic overtraining.</p>

<h2 id="so-pre-train-for-more-than-just-performance">So, pre-train for more than just performance</h2>

<p>For small (and overtrained) models, there’s a real trade-off between minimizing pre-training loss and preserving adaptability. If the only objective is a better validation loss on the base model, you can end up with a system that’s harder to adapt and less robust after post-training. In short: <strong>don’t just train for performance—train for adaptability</strong>.</p>]]></content><author><name>Jacob Springer</name></author><category term="research" /><category term="training" /><category term="adaptability" /><summary type="html"><![CDATA[You’ve probably seen confident claims on social media by prominent AI personalities that we already have the ingredients for AGI and simply need to scale. Proponents of this claim often point to one of many graphs illustrating the release date of a model plotted against some measure of performance, showing an exponential grown in capability over time. It’s true that today’s language models are impressively capable. However, as I will argue in this post, when you actually try to use language models in practice, especially when you want to adapt them to new tasks, the picture becomes messier.]]></summary></entry><entry><title type="html">Weights could be helpful for interpretation</title><link href="https://ar-forum.github.io/blog/weights-could-be-helpful-for-interpretation/" rel="alternate" type="text/html" title="Weights could be helpful for interpretation" /><published>2025-10-16T20:00:00-04:00</published><updated>2025-10-16T20:00:00-04:00</updated><id>https://ar-forum.github.io/blog/weights-could-be-helpful-for-interpretation</id><content type="html" xml:base="https://ar-forum.github.io/blog/weights-could-be-helpful-for-interpretation/"><![CDATA[<p>Imagine you’ve just downloaded an open-source model for your agentic system, but you’re worried it might contain hidden backdoors. You’d like to use interpretability techniques to check, but there’s a problem: most methods like SAEs require you to already have example inputs that trigger the backdoor<sup id="fnref:1"><a href="#fn:1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>, creating a chicken-and-egg situation.</p>

<p>But if someone added a backdoor through fine-tuning, that process had to change the weights somehow. Those changes might leave traces: patterns in the weight differences that encode the backdoor behavior. <strong>Can we detect suspicious behavior by looking at the model weights themselves?</strong> In typical fine-tuning setups, we show that the answer is yes.</p>

<h2 id="our-method"><strong>Our method</strong></h2>

<p>To find these traces, we look at what changed during fine-tuning. Here’s how it works.</p>

<p><strong>Step 1: Find the important directions</strong> Instead of analyzing activations (like traditional interpretability methods), we analyze the weight differences between the pre-finetuned and post-finetuned model. Specifically, we:</p>
<ul>
  <li>Focus on the attention projection matrices ($O_{\text{proj}}$) and MLP down-projection matrices ($M_{\text{down}}$) in LLMs, as they directly write to the residual stream</li>
  <li>Calculate the difference between the fine-tuned and original versions of these matrices</li>
  <li>Run SVD on these differences to find the top singular vectors</li>
</ul>

<p>These singular vectors capture the main directions of what changed during fine-tuning. If a backdoor was added, one of these directions could encode that backdoor behavior.</p>

<figure>
<img src="/assets/images/weight-watch-histogram.png" alt="Histogram of activation cosine similarity with 11-th singular vector of Delta O_proj 4" />
<figcaption>Figure 1: Histogram of activation cosine similarity with 11-th singular vector of $\Delta O_{proj, 4}$. Prompts examined are benign, harmful, and them with backdoor trigger added.</figcaption>
</figure>

<p>In the figure above, we tested malicious prompts from <em>StrongREJECT</em> alongside syntactically similar benign prompts on a model with backdoor injected during PPO. When we add the backdoor trigger, the activations cleanly separate. The SVD direction does capture the backdoor behavior.</p>

<p><strong>Step 2: Monitor activations at runtime</strong> At inference time, we check how strongly each input activates these directions by computing cosine similarity. If a direction corresponds to a backdoor, inputs with the trigger should have very different similarity scores compared to normal inputs.</p>

<p><strong>Step 3: Flag anomalies</strong> We maintain a running range of “normal” similarity scores for each direction. If an input’s similarity falls outside this range<sup id="fnref:2"><a href="#fn:2" class="footnote" rel="footnote" role="doc-noteref">2</a></sup>, we flag it as anomalous. The system adapts over time, updating the normal range as it sees more data. You can also collect the most extreme examples (highest/lowest similarities) for further investigation.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_directions</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">each</span> <span class="n">layer</span><span class="p">:</span>
        <span class="n">compute</span> <span class="n">top</span> <span class="n">singular</span> <span class="n">vectors</span> <span class="n">of</span> <span class="err">△</span><span class="n">Oproj</span> <span class="ow">and</span> <span class="err">△</span><span class="n">Mdown</span>

<span class="k">def</span> <span class="nf">inference</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">each</span> <span class="n">layer</span><span class="p">:</span>
        <span class="n">collect</span> <span class="n">activation</span> <span class="n">a</span> <span class="n">at</span> <span class="n">this</span> <span class="n">layer</span>
        <span class="k">for</span> <span class="n">each</span> <span class="n">top</span> <span class="n">singular</span> <span class="n">vector</span> <span class="n">v</span> <span class="n">of</span> <span class="n">this</span> <span class="n">layer</span><span class="p">:</span>
            <span class="n">compute</span> <span class="n">cosine</span> <span class="n">similarity</span> <span class="nf">of </span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span>
            <span class="n">flag</span> <span class="k">as</span> <span class="n">anomalous</span> <span class="k">if</span> <span class="n">it</span> <span class="n">goes</span> <span class="n">out</span> <span class="n">of</span> <span class="n">the</span> <span class="n">running</span> <span class="nb">range</span>
            <span class="n">update</span> <span class="n">running</span> <span class="nb">range</span>
</code></pre></div></div>

<h2 id="how-well-does-it-work"><strong>How well does it work?</strong></h2>

<p>We tested this method for detecting backdoor utilizations and unlearned content in simulated production traffic (mixing WildChat and UltraChat data).</p>

<p><strong>Backdoor detection</strong>: Our method catches the first backdoor attempt in the data stream with 56-100% accuracy at ~1% false positive rate. For 4 out of the 9 backdoor setups we tested, we consistently detected the backdoor on the very first trigger.</p>

<p><strong>Unlearning detection</strong>: For 3 models that had undergone unlearning, we could spot when they encountered unlearned content with 36-95% accuracy and ~2% false positive rate.</p>

<p><strong>Real-world models</strong>: We also analyzed actual open-weight models like OLMo, Qwen, and Llama. We can’t definitively prove or disprove backdoors (we might just not have the right triggers), but we found some interesting patterns. OLMo had many directions related to Midjourney prompts and marketing content, and we indeed found similar patterns on ShareGPT data which was used in its fine-tuning. Check out our <a href="https://fjzzq2002.github.io/WeightWatch/visualizer.html">auto-interp visualizer for Qwen 2.5 7B</a> to explore interesting directions.</p>

<h1 id="limitations-and-takeaways"><strong>Limitations and takeaways</strong></h1>

<p>This is one of the first steps toward weight-based interpretability (also checkout the concurrent <a href="https://arxiv.org/abs/2506.20790">stochastic parameter decomposition</a>). Our work is pretty coarse-grained: we’re assuming SVD gives us the right basis, we can’t handle superposition well, and we don’t fully capture mechanistic details, but it’s cheap and works surprisingly well for our settings. We think it could be a useful addition to the monitoring toolbox, especially as a first line of defense. Taking a step back, we believe there is ample room in better utilizing weights for interpretability, and we hope this work can serve as a stepping stone.</p>

<table>
  <tbody>
    <tr>
      <td>📄 <a href="https://arxiv.org/abs/2508.00161">Paper</a></td>
      <td>💻 <a href="https://github.com/fjzzq2002/WeightWatch">Code</a></td>
      <td>🌐 <a href="https://fjzzq2002.github.io/WeightWatch">Website</a></td>
    </tr>
  </tbody>
</table>
<div class="footnotes" role="doc-endnotes">
  <ol>
    <li id="fn:1">
      <p>For example, an instruct-model SAE trained on base-model data <a href="https://www.lesswrong.com/posts/rtp6n7Z23uJpEH7od/saes-are-highly-dataset-dependent-a-case-study-on-the">struggles to find features related to refusal</a>. <a href="#fnref:1" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
    <li id="fn:2">
      <p>One may also adjust these ranges to certain percentiles to control for false positive rates. <a href="#fnref:2" class="reversefootnote" role="doc-backlink">&#8617;</a></p>
    </li>
  </ol>
</div>]]></content><author><name>Ziqian Zhong</name></author><category term="research" /><category term="interpretability" /><summary type="html"><![CDATA[Imagine you’ve just downloaded an open-source model for your agentic system, but you’re worried it might contain hidden backdoors. You’d like to use interpretability techniques to check, but there’s a problem: most methods like SAEs require you to already have example inputs that trigger the backdoor1, creating a chicken-and-egg situation. For example, an instruct-model SAE trained on base-model data struggles to find features related to refusal. &#8617;]]></summary></entry><entry><title type="html">Welcome to our blog!</title><link href="https://ar-forum.github.io/blog/welcome/" rel="alternate" type="text/html" title="Welcome to our blog!" /><published>2025-09-01T08:49:00-04:00</published><updated>2025-09-01T08:49:00-04:00</updated><id>https://ar-forum.github.io/blog/welcome</id><content type="html" xml:base="https://ar-forum.github.io/blog/welcome/"><![CDATA[<p>Welcome to the our blog! The goal of this blog is to make our research more accessible and to engage with the broader ML community. Stay tuned!</p>]]></content><author><name>Students</name></author><category term="announcements" /><summary type="html"><![CDATA[Welcome to the our blog! The goal of this blog is to make our research more accessible and to engage with the broader ML community. Stay tuned!]]></summary></entry></feed>